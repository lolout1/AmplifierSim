# PySpice is used for building and running SPICE simulations.
PySpice>=1.5.0

# NumPy is used for numerical operations.
numpy>=1.19.0

# Matplotlib is used for plotting the simulation results.
matplotlib>=3.3.0

# (Optional) cffi is needed by PySpice for interfacing with NGSPICE.
cffi>=1.14.0

